import React from 'react';

import UploadForm from './UploadForm';

function Home() {
    return (
        <main className="App">
            <header className="App-header">
                <UploadForm />
            </header>
        </main>
    );
}

export default Home;
